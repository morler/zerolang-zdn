fn left<T: Type>(value: T) -> Void {
    let nested: Maybe<T> = null
    right(nested)
}

fn right<T: Type>(value: T) -> Void {
    let nested: Maybe<T> = null
    left(nested)
}

pub fn main(world: World) -> Void raises {
    left<i32>(1)
    check world.out.write("mutual inferred polymorphic recursion ok\n")
}
