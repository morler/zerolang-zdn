type Counter {
    value: i32,
}

fn readValue<T: Missing<T>>(value: ref<T>) -> i32 {
    return 0
}

pub fn main() -> Void {
    let counter: Counter = Counter { value: 1 }
    expect (readValue<Counter>(&counter) == 1)
}
