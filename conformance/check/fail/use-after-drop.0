type File {
    fd: i32,
}

fn consume(file: owned<File>) -> Void {
    return
}

pub fn main(world: World) -> Void raises {
    let file: owned<File> = File { fd: 1 }
    file.drop()
    consume(file)
    check world.out.write("use after drop\n")
}
