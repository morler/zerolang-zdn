type Point {
    x: i32,
}

fn bump_x(point: mutref<Point>) -> Void {
    point.x = point.x + 1
}

pub fn main(world: World) -> Void raises {
    let point: Point = Point { x: 1 }
    bump_x(&mut point)
    check world.out.write("bad mutref\n")
}
