const cap: usize = 3

fn head<T: Type, static N: usize>(items: ref<[N]T>) -> T {
    return items[0]
}

pub fn main() -> Void {
    let values: [cap]i32 = [3, 4, 5]
    let first: i32 = head(&values)
    expect (first == 3)
}
