fn storeLocal(slot: MutSpan<ref<i32>>) -> Void {
    let local: i32 = 1
    slot[0] = &local
}

pub fn main() -> Void {
    let fallback: i32 = 0
    var slots: [1]ref<i32> = [&fallback]
    storeLocal(slots)
}
