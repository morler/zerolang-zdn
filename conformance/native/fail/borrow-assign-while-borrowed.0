type Point {
    x: i32,
}

pub fn main(world: World) -> Void raises {
    var point: Point = Point { x: 1 }
    let shared: ref<Point> = &point
    point.x = 2
    check world.out.write("assigned while borrowed")
}
