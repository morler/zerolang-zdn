fn value() -> i32 raises [Bad] {
    raise Bad
}

pub fn main(world: World) -> Void raises {
    let item: i32 = rescue value() err "bad"
    if item == 0 {
        check world.out.write("unreachable\n")
    }
}
