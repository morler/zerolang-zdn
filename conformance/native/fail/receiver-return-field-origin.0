shape Box {
    value: ref<i32>,
    count: i32,

    fun countRef(self: ref<Self>) -> ref<i32> {
        return &self.count
    }
}

pub fun main() -> Void {
    let x = 1
    let mut box = Box { value: &x, count: 0 }
    let current = box.countRef()
    box.count = 1
}
