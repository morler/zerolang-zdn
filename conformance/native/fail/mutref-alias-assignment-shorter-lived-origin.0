type Holder {
    value: ref<i32>,
}

pub fn main() -> Void {
    let old: i32 = 1
    var holder: Holder = Holder { value: &old }
    let selected: mutref<Holder> = &mut holder
    if true {
        let inner: i32 = 2
        selected.value = &inner
    }
}
