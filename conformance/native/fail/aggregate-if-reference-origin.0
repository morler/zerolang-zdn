type Box {
    value: ref<i32>,
}

pub fn main(flag: Bool) -> Void {
    var x: i32 = 1
    let y: i32 = 2
    var box: Box = Box { value: &x }
    if flag {
        box = Box { value: &y }
    }
    x = 3
}
