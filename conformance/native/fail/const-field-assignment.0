type Wrapper {
    value: const i32,
}

pub fn main(world: World) -> Void raises {
    var wrapper: Wrapper = Wrapper { value: 1 }
    wrapper.value = 2
    check world.out.write("assigned const field")
}
