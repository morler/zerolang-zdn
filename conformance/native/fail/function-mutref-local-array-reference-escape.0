type Holder {
    values: [1]ref<i32>,
}

fn storeLocal(holder: mutref<Holder>) -> Void {
    let local: i32 = 1
    holder.values = [&local]
}

pub fn main() -> Void {
    let fallback: i32 = 0
    var holder: Holder = Holder { values: [&fallback] }
    storeLocal(&mut holder)
}
