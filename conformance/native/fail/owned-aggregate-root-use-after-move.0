type File {
    fd: i32,
}

type Holder {
    file: owned<File>,
}

fn consume(holder: Holder) -> Void {
}

pub fn main() -> Void {
    let file: owned<File> = File { fd: 1 }
    let first: Holder = Holder { file: file }
    let second: Holder = first
    consume(first)
}
