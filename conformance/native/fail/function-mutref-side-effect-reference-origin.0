type Holder {
    value: ref<i32>,
}

fn replace(holder: mutref<Holder>, next: ref<i32>) -> Void {
    holder.value = next
}

pub fn main() -> Void {
    var x: i32 = 1
    let y: i32 = 2
    var holder: Holder = Holder { value: &y }
    replace(&mut holder, &x)
    x = 3
}
