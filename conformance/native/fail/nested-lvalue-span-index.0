type View {
    values: Span<i32>,
}

pub fn main(world: World) -> Void raises {
    let values: [2]i32 = [1, 2]
    var view: View = View { values: values }
    view.values[0] = 3
    check world.out.write("bad nested lvalue\n")
}
