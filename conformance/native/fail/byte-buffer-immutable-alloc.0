pub fn main(world: World) -> Void raises {
    var storage: [8]u8 = [0, 0, 0, 0, 0, 0, 0, 0]
    let alloc: FixedBufAlloc = std.mem.fixedBufAlloc(storage)
    let maybe: i32 = std.mem.byteBuf(alloc, 4)
    check world.out.write("immutable byte buffer allocator")
}
