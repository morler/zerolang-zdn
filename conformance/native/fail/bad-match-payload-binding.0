choice Result {
    ok: i32,
    done,
}

pub fn main(world: World) -> Void raises {
    let result: Result = Result.done
    match result {
        .ok(value) {
            check world.out.write("ok\n")
        }
        .done(value) {
            check world.out.write("done\n")
        }
    }
}
