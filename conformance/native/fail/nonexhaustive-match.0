enum Status {
    ready,
    failed,
}

pub fn main(world: World) -> Void raises {
    let status: Status = Status.ready
    match status {
        ready {
            check world.out.write("ready\n")
        }
    }
}
