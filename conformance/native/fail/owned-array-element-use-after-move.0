type File {
    fd: i32,
}

fn close(file: owned<File>) -> Void {
}

pub fn main(world: World) -> Void raises {
    let file: owned<File> = File { fd: 1 }
    let files: [1]owned<File> = [file]
    close(file)
    check world.out.write("owned array element move")
}
