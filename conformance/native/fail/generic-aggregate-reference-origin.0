type Box {
    value: ref<i32>,
}

fn id<T: Type>(value: T) -> T {
    return value
}

pub fn main() -> Void {
    var x: i32 = 1
    let y: i32 = 2
    var box: Box = Box { value: &x }
    let copy: Box = id(box)
    box = Box { value: &y }
    x = 3
}
