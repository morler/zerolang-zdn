type Point {
    x: i32,
}

type Other {
    value: i32,
}

fn read_x(point: ref<Point>) -> i32 {
    return point.x
}

pub fn main(world: World) -> Void raises {
    let other: Other = Other { value: 1 }
    let value: i32 = read_x(&other)
    check world.out.write("wrong borrow type")
}
