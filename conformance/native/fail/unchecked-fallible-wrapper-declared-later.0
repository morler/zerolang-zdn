type Counter {
    value: i32,
    fn risky(self: ref<Self>) -> Void raises [Bad] {
        raise Bad
    }
}

pub fn main() -> Void {
    let counter: Counter = Counter { value: 0 }
    wrapper(&counter)
}

fn wrapper(counter: ref<Counter>) -> Void raises {
    check counter.risky()
}
