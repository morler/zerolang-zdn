type Point {
    x: i32,
    y: i32,
}

fn field(point: ref<Point>) -> ref<i32> {
    return &point.x
}

pub fn main() -> Void {
    var point: Point = Point { x: 1, y: 2 }
    let current: ref<i32> = field(&point)
    point.y = 3
}
