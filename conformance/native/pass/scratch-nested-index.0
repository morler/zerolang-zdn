fn idx() -> usize {
    return 0
}

pub fn main(world: World) -> Void raises {
    let values: [1]u32 = [2]
    let sum: u32 = 40_u32 + values[idx() + 0]
    if sum == 42_u32 {
        check world.out.write("scratch nested index ok\n")
    }
}
