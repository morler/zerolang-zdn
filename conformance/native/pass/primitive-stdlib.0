use std.codec

use std.parse

pub fn main(world: World) -> Void raises {
    let len: usize = std.codec.encodedVarintLen(300)
    let digit: Bool = std.parse.isAsciiDigit("7")
    if len == 2 && digit {
        check world.out.write("native primitive std")
    }
}
