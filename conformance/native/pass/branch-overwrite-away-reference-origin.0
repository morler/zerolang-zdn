type Holder {
    value: ref<i32>,
}

pub fn main(flag: Bool) -> Void {
    var old: i32 = 1
    let next: i32 = 2
    var holder: Holder = Holder { value: &old }
    if flag {
        holder.value = &next
    } else {
        holder.value = &next
    }
    old = 3
}
