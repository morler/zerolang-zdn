type Holder {
    value: ref<i32>,
}

fn store(holder: mutref<Holder>, next: ref<i32>) -> Void {
    holder.value = next
}

pub fn main() -> Void {
    let old: i32 = 1
    let next: i32 = 2
    var holder: Holder = Holder { value: &old }
    store(&mut holder, &next)
}
