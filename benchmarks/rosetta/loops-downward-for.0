pub fn main(world: World) -> Void raises {
    var i: i32 = 5
    var sum: i32 = 0
    while i > 0 {
        sum = sum + i
        i = i - 1
    }
    if sum == 15 {
        check world.out.write("downward loop ok\n")
    }
}
