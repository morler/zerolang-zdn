fn row(index: u8) -> u8 {
    return index / 5
}

fn col(index: u8) -> u8 {
    return index % 5
}

pub fn main(world: World) -> Void raises {
    let index: u8 = 7
    if row(index) == 1 && col(index) == 2 {
        check world.out.write("bifid ok\n")
    }
}
