pub fn main(world: World) -> Void raises {
    let values: [4]u32 = [1, 2, 3, 4]
    var i: usize = 0
    var sum: u32 = 0
    while i < 4 {
        sum = sum + values[i]
        i = i + 1
    }
    if sum == 10 {
        check world.out.write("foreach loop ok\n")
    }
}
