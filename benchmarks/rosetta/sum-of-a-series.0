fn squareSeries(n: u32) -> u32 {
    var total: u32 = 0
    var i: u32 = 1
    while i <= n {
        total = total + i * i
        i = i + 1
    }
    return total
}

pub fn main(world: World) -> Void raises {
    if squareSeries(5) == 55 {
        check world.out.write("series ok\n")
    }
}
