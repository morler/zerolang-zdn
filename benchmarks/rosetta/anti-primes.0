fn antiPrime Bool n u32
  let target std.math.divisorCountU32 n
  mut value u32 1
  while < value n
    if >= (std.math.divisorCountU32 value) target
      ret false
    set value + value 1
  ret true

pub fn main Void world World !
  if antiPrime 12
    check world.out.write "anti prime ok\n"
