fn relax(current: i32, left: i32, right: i32) -> i32 {
    let via: i32 = left + right
    if via < current {
        return via
    }
    return current
}

pub fn main(world: World) -> Void raises {
    if relax(9, 2, 3) == 5 {
        check world.out.write("floyd warshall ok\n")
    }
}
