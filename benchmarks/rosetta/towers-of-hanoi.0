fn moves(disks: u32) -> u32 {
    var total: u32 = 1
    var i: u32 = 0
    while i < disks {
        total = total * 2
        i = i + 1
    }
    return total - 1
}

pub fn main(world: World) -> Void raises {
    if moves(4) == 15 {
        check world.out.write("hanoi ok\n")
    }
}
