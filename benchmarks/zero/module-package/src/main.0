use math

use text

pub fn main(world: World) -> Void raises {
    var i: i32 = 0
    var total: i32 = 0
    while i < 50000 {
        total = total + mix(i)
        i = i + 1
    }
    if total > 0 && labelOk() {
        check world.out.write("module package\n")
    }
}
