type Token {
    kind: u8,
    start: usize,
    len: usize = 1,
}

export c fn main() -> u8 {
    var token: Token = Token { kind: 65_u8, start: 2 }
    token.len = token.start + token.len
    if token.len == 3 {
        return token.kind
    }
    return 0_u8
}
