export c fn main() -> i32 {
    var values: [4]i32 = [0, 0, 0, 0]
    var i: usize = 0
    var value: i32 = 2
    while i < 4 {
        values[i] = value
        value = value + 2
        i = i + 1
    }
    return values[0] + values[1] + values[2] + values[3]
}
