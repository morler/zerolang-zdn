fn sum_to(n: i32) -> i32 {
    var i: i32 = 0
    var sum: i32 = 0
    while i <= n {
        sum = sum + i
        i = i + 1
    }
    return sum
}

export c fn main() -> i32 {
    return sum_to(4)
}
